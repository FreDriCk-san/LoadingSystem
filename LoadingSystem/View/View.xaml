<UserControl x:Class="LoadingSystem.View.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wpfTool="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:LoadingSystem.View"
             mc:Ignorable="d" 
             d:DesignHeight="757" d:DesignWidth="1081">
    <Grid>
        <Menu HorizontalAlignment="Left" Height="21" VerticalAlignment="Top" Width="1070">
            <MenuItem Header="Файл">
                <MenuItem Header="Открыть" Command="{Binding FileOpenCommand}"/>
            </MenuItem>
        </Menu>


        <wpfTool:PropertyGrid x:Name="propGrid" Width="300" Margin="763,27,10,329" SelectedObject="{Binding PropertyGridModel, UpdateSourceTrigger=PropertyChanged}" />


        <TextBox x:Name="textBoxInfo" IsReadOnly="True" 
                 HorizontalAlignment="Left" Height="370" 
                 Margin="10,27,0,0" 
                 Text="{Binding TextBoxData, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                 VerticalAlignment="Top" Width="743"/>


        <Grid x:Name="gridOfProperties" HorizontalAlignment="Left" Height="92" Margin="10,414,0,0" VerticalAlignment="Top" Width="1050">
            <TextBox Text="Name" Margin="0,0,1009,71"/>
            <TextBox Text="Type" Margin="0,21,1009,50"/>
            <TextBox Text="Unit" Margin="0,42,1009,29"/>
            <ScrollViewer x:Name="stackPanelScroll" HorizontalScrollBarVisibility="Hidden" Margin="41,0,0,0">
                <StackPanel x:Name="propertyPanel" Orientation="Horizontal"/>
            </ScrollViewer>
        </Grid>
        
        
        <DataGrid x:Name="gridOfData" HorizontalAlignment="Left" Height="205" 
                  Margin="10,512,0,0" VerticalAlignment="Top" Width="1050" 
                  ColumnWidth="110" RowHeaderWidth="40" 
                  ItemsSource="{Binding DefaultTableView, UpdateSourceTrigger=PropertyChanged}" 
                  LoadingRow="DataGrid_LoadingRow" ScrollViewer.ScrollChanged="GridOfData_ScrollChanged" 
                  Loaded="GridOfData_Loaded"/>

    </Grid>
</UserControl>
